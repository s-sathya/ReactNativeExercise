import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonGrad: {
    borderRadius: 10,
    position: "absolute",
    backgroundColor: "#a424a4",
    borderColor: "#800080",
    borderWidth: 3,
    bottom: 5,
  },
  buttonParent: {
    borderRadius: 15,
    backgroundColor: "#3a8a3a",
  },
});
